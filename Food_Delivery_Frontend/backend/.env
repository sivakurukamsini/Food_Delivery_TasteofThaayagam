## Backend environment configuration
## Keep secrets out of source control. This file lives on your local dev machine.

# MongoDB connection (already configured for this project)
MONGO_URI="mongodb+srv://ToT_admin:0000@cluster0.pq5hvg7.mongodb.net/food-delivery?retryWrites=true&w=majority&appName=Cluster0"

# Server port
PORT=5001

# JWT secret used to sign auth tokens
JWT_SECRET="random#secret"

# Stripe (if used)
STRIPE_SECRET_KEY="sk_test_51S3LZOEwSE9WyCXqQekj9u7XjVZ9yesMtTMGQfkaUdE8ne7ZaWhABG8jy08J6HzlFvQ91x2Ttflnr7lzkZkx5ac300va5JPzrd"

################################################################################
# SMTP / Email configuration
# To send real emails via Gmail you must:
# 1) Enable 2FA on the Gmail account
# 2) Create an App Password (16 characters) at https://myaccount.google.com/apppasswords
# 3) Paste the Gmail address into SMTP_USER and the app password into SMTP_PASS
# Nodemailer will use these creds. If these are not provided, the app falls back to Ethereal
# (dev preview) and prints a preview URL in server logs / responses.
################################################################################

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
# Use 'true' when using port 465 with SSL, otherwise keep 'false' for STARTTLS (port 587)
SMTP_SECURE=false

# Replace with your gmail address, e.g. 'myaccount@gmail.com'
SMTP_USER=yourgmail@gmail.com
# Replace with the 16-character app password (do NOT use your regular Gmail password)
SMTP_PASS=your_16_char_app_password_here

# From address shown in emails
FROM_EMAIL=yourgmail@gmail.com

# Frontend app URL used in some email links
APP_URL=http://localhost:5173

# End of .env
DEBUG_EMAIL=true
# SMTP
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=kamsmc8@gmail.com
SMTP_PASS=<paste-your-16-char-app-password-here>
FROM_EMAIL=kamsmc8@gmail.com

# For helpful debugging during setup:
DEBUG_EMAIL=true
APP_URL=http://localhost:5173